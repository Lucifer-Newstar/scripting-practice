## Introduction:
Unlike the readme in py-practice repo I'd like to keep this a bit more formal. Can't always be wannabe funny everywhere tbh.
Getting into the context as you should know my the repo name, this repo features my struggles to conquer bash scrripting perhaps

## Let's pretend you're NOT dumb:
do point out if I made mistakes
to clone this repo which no one would : git clone "too lazy to put the link here please do it yourself " 
to run the script file: 
```bash
bash filename.sh
```
*You can also make the script executable using `chmod +x filename.sh` and run it via `./filename.sh`.*
